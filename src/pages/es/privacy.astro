---
import {PROJECT_NAME} from "../../config";
import MainLayout from "../../layouts/main.astro";
import {PrivacyPolicyPage} from "../../components";
import {AppWrapper} from "../../layouts";
---
<MainLayout content={{title: `${PROJECT_NAME} - Política de privacidad`}}>
    <AppWrapper client:only="react">
        <PrivacyPolicyPage client:only="react"/>
    </AppWrapper>
</MainLayout>
