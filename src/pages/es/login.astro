---
import MainLayout from "../../layouts/main.astro";
import {LoginPage} from "../../components/pages/Login";
import {PROJECT_NAME} from "../../config";
---
<MainLayout content={{title: `${PROJECT_NAME} - Acceso`}}>
    <LoginPage client:only="react"/>
</MainLayout>